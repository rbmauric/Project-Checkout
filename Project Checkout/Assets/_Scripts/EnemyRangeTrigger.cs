using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyRangeTrigger : MonoBehaviour
{
  /*

    private void OnTriggerEnter2D(Collider2D attack)
    {
        
        if (attack.CompareTag("Player"))
        {
            // attack.GetComponent<Player>().Damage();
            attack.GetComponent<RangeEnemy>().RangeTrigger();
            attack.GetComponent<RangeEnemy>().Shoot();
        }
    }

    private void OnTriggerExit2D(Collider2D _OoB)
    {
        if (_OoB.CompareTag("Player"))
        {
            _OoB.GetComponent<RangeEnemy>().OoBTrigger();
        }
    }
    */
}
